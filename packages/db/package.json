{
  "name": "db",
  "version": "1.0.0",
  "license": "UNLICENSED",
  "private": true,
  "scripts": {
    "commit": "DATABASE_URL=postgres://postgres:abacaba@localhost/mmc SHADOW_DATABASE_URL=postgres://postgres:abacaba@localhost/mmc_shadow ROOT_DATABASE_URL=postgres://postgres:abacaba@localhost/postgres graphile-migrate commit",
    "migrate": "DATABASE_URL=postgres://postgres:abacaba@localhost/mmc SHADOW_DATABASE_URL=postgres://postgres:abacaba@localhost/mmc_shadow ROOT_DATABASE_URL=postgres://postgres:abacaba@localhost/postgres graphile-migrate migrate",
    "uncommit": "DATABASE_URL=postgres://postgres:abacaba@localhost/mmc SHADOW_DATABASE_URL=postgres://postgres:abacaba@localhost/mmc_shadow ROOT_DATABASE_URL=postgres://postgres:abacaba@localhost/postgres graphile-migrate uncommit",
    "seed": "DATABASE_URL=postgres://postgres:abacaba@localhost/mmc SHADOW_DATABASE_URL=postgres://postgres:abacaba@localhost/mmc_shadow ROOT_DATABASE_URL=postgres://postgres:abacaba@localhost/postgres graphile-migrate run src/seeds.sql"
  },
  "engines": {
    "yarn": ">=1.22.0 <2.0.0",
    "node": ">=17.0.0"
  },
  "devDependencies": {
    "graphile-migrate": "^1.4.0"
  }
}
