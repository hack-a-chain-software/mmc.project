[package]
name = "vesting-contract"
version = "1.0.0"
authors = ["vote for pedro"]
edition = "2018"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
near-sdk = { workspace = true }

serde = "1"
serde_json = "1"
borsh = "0.9.3"

[dev-dependencies]
rstest = "0.16"
